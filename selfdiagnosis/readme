# Take a look the Cluster-IP/NodePort with respect to RabbitMq in the Cluster K8s
# kubectl --namespace="test-rabbitmq"

# Remenber to use kubectl config set-context --current --namespace=test-rabbitmq
# The protocol amqp must be verified by executing "kubectl describe service rabbitmq"    --- e.g. amqp://guest:guest@172.17.0.3:5672/%2f'
# api rest
docker build -t selfdiagnosis-python-version .
docker tag selfdiagnosis-python-version californibrs/selfdiagnosis-python-version
docker push californibrs/selfdiagnosis-python-version
# (dont use -- only during developing) docker run -it --rm -p 3000:80 selfdiagnosis-python-version



## Enabling the ingress to use DNS - Minikube
minikube addons enable ingress


# the cluster IP must be included in the hosts

# the cluster IP must be included in the hosts

kubectl get ingress
<ip-address> microcontrollers.com